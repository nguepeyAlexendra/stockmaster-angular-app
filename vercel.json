{
  "buildCommand": "npm run build -- --configuration=production",
  "outputDirectory": "dist/stockmaster-frontend/browser",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}